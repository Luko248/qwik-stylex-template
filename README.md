# qwik-stylex-template
 Template for creating a new Qwik website using Vite, StyleX and SCSS.

 Just run in project root folder:
 `pnpm i`
 
 For development use:
 `pnpm run dev`
 
 For production use:
 `pnpm run build`
 
